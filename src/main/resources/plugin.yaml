# Refer https://docs.halo.run/developer-guide/plugin/basics/manifest

apiVersion: plugin.halo.run/v1alpha1
kind: Plugin
metadata:
  # The name defines how the plugin is invoked, A unique name
  name: api-configstore
spec:
  enabled: true
  requires: ">=2.20.0"
  author:
    name: sky
    website: https://www.5ee.net
  logo: logo.png
  homepage: https://github.com/sky121666/plugin-APIConfigStore#readme
  repo: https://github.com/sky121666/plugin-APIConfigStore
  issues: https://github.com/sky121666/plugin-APIConfigStore/issues
  displayName: "API配置存储"
  description: "将其他插件的配置通过它们的api地址获取后存储到数据库内，在由后端通过finder让主题端可以使用标签调用对应的配置"
  license:
    - name: "GPL-3.0"
      url: "https://github.com/halo-dev/plugin-starter/blob/main/LICENSE"
